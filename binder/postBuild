cd binder

mkdir ~/.jupyter
mv jupyter_jupyterlab_server_config.py ~/.jupyter/jupyter_jupyterlab_server_config.py

python -c "from project import project; project.setup_files()"
python -c "from project import project; project.setup_files()"
