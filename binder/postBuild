cd binder

mkdir ~/.jupyter
mv jupyter_jupyterlab_server_config.py ~/.jupyter/jupyter_lab_config.py

export PYTHONPATH='~/binder'

python -c "from project import project; project.setup_files()"
python -c "from project import project; project.setup_files()"
